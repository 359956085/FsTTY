# 依赖安全审计基线

复查日期：2026-09-10。

## 当前豁免

- `RUSTSEC-2026-0194`、`RUSTSEC-2026-0195`：`quick-xml 0.39.4` 由 `plist 1.9.0` 的 `^0.39.2` 约束引入。直接升级到修复版 `0.41.0` 无法解析依赖；等待 Tauri/plist 上游放宽约束。
- `RUSTSEC-2023-0071`：`rsa 0.10.0-rc.18` 由 `russh 0.62.2` 引入，RustSec 当前未提供修复版本。FsTTY 不生成 SSH 服务端 RSA 私钥，但仍需跟踪 russh 上游替换进度。

## 已处理

- `RUSTSEC-2026-0221`：`event-listener` 已从 `5.4.1` 升至 `5.4.2`。
- 前端生产依赖审计当前无告警。

CI 固定使用 `cargo-audit 0.22.2` 并读取 `.cargo/audit.toml`。豁免外任何新漏洞会阻断质量检查。

